class TeamsAuth:
    def __init__(self, bearer_token, skype_token, sharepoint_token):
        self.bearer_token = bearer_token
        self.skype_token = skype_token
        self.sharepoint_token = sharepoint_token
